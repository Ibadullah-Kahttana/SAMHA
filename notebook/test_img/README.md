Place a test image here to try the SAMHA inference pipeline in
`notebook/run_samha.ipynb` — for example an H&E-stained histopathology tile —
and, optionally, its matching ground-truth mask.

Then set `IMAGE_NAME` (and `GT_NAME`, if you have a mask) in Cell 3 of the
notebook to your filenames.
